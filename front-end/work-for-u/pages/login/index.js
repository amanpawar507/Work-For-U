import { Layout } from "../../components"
import { Login } from "../../components/login"

const LoginPage = () => {
    return (
        <Layout>
            <Login/>
        </Layout>
    )
}

export default LoginPage